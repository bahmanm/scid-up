# Enforce LF line endings for test fixtures.
# This prevents stray '\r' bytes on Windows checkouts, which break tests that
# intentionally read fixtures in binary mode.

gtest/*.pgn text eol=lf
gtest/*.txt text eol=lf
